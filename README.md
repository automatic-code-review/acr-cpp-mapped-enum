# acr-cpp-mapped-enum

Arquivo config.json

```json
{
    "stage": "static",
    "type": "GOOGLE_SHEETS",
    "message": "${ENUM_NAME} - ${FILE_NAME} - ${ENUM_VALUE}",
    "enums": [
        "project-name/EnumName",
    ],
    "data": {
        "credentials": {
        },
        "sheetId": ""
    }
}
```
